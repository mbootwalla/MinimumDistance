#include <R.h>
#include <Rinternals.h>

/********void joint1c (double loglik,
	      integer trio.states
	      double tau,
	      double log.pi,
	      integer normal.index,
	      integer segment.index,
	      double table1,
	      double table3,
	      integer is.denovo,
	      double Prob.DN,
	      integer denovo.prev,
	      integer state.prev) {
  int state;
  for(i=0; i<3; i++){
    state[i] = trio.states[state.index][i];
    printf(state[i])
  }
}

/*(double *a, double M[5][5][5][5][5][5])*/
