useDynLib("Beaty", .registration=TRUE)
importFrom(graphics, abline, axis, layout, legend, mtext, par, plot,
	   polygon, rect, segments, text)

importClassesFrom(Biobase, eSet)
importMethodsFrom(Biobase, annotatedDataFrameFrom, updateObject)
importFrom(Biobase, assayDataElementReplace, sampleNames, varLabels, rowMedians)

##importFrom(mybase, unique)
importFrom(stats, mad)


importClassesFrom(IRanges, RangedData, IRanges, DataTable, Vector, Annotated, List, DataTableORNULL)
importMethodsFrom(IRanges, start, end, "[", "[[","[<-", "[[<-", "$")##, stack, rbind)
importFrom(IRanges, IRanges, RangedData, RangedDataList)

importMethodsFrom(oligoClasses, open, close)
importFrom(oligoClasses, isFF, getBar)

importFrom(ff, filename)

importFrom(DNAcopy, CNA, segment, smooth.CNA, getbdry)

importFrom(lattice, xyplot)

##exportPattern("^[^\\.]")
exportClasses(DataFrameCNV, RangedDataCNV, TrioSet, LogRatioSet, TrioSetList)
exportMethods(plot, nMarkers, state, copyNumber, open, close,
	      calculateMindist, mindist, "mindist<-", updateObject,
	      logR, baf, "logR<-", "baf<-", mad, "mad<-", xsegment, prune,
	      phenoData2, "phenoData2<-", ncol, RangedDataCNV,
	      xyplot,
	      offspringNames,
	      fatherNames,
	      motherNames,
	      computeBayesFactor)##, stack, rbind)
export(beadstudiodir,
       completeTrios,
       fmoNames,
       fullId,
       getAllBayesFactorRanges,
       getKey,
       harmonizeStates
       isDenovo,
       loadBatchFiles,
       my.xypanel,
       pennDenovoFreq,
       pennStats,
       prepanel.fxn,
       readPedfile,
       readPennCnv,
       ssampleNames, sssampleNames, ss, s,
       wrapperPosteriorProbs)


